{
  "name": "custom-element-template",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "document": "jsdoc --destination wiki/docs --readme README.md dist",
    "build": "npm run typecheck && vite build && npm run document",
    "preview": "vite preview",
    "rename": "node scripts/rename.mjs"
  },
  "devDependencies": {
    "jsdoc": "^4.0.4",
    "less": "^4.2.0",
    "typescript": "^5.5.4",
    "vite": "^5.0.0"
  }
}
